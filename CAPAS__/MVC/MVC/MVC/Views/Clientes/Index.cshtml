@model IEnumerable<MVC.Models.Cliente>

@{
    ViewBag.Title = "Index";
}

<h2>Clientes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nom_cliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cell_cliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.gmail_cliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nom_cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cell_cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.gmail_cliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cod_cliente }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cod_cliente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cod_cliente })
        </td>
    </tr>
}

</table>
